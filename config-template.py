name = username
dbname = databasename
host = localhost
password = password

# change the file name to config.py, and the variables to your "username" etc. Import this to your
# main python file, and refer like : connect_str = "dbname = %s user = %s host = %s password = %s" % (config.dbname, config.name, config.host, config.password)
